// Simulated data (replace with real data)
const schemesCount = 10;
const contactsCount = 50;
const pendingRequests = 3;
const rejectedCount = 2;
const approvedCount = 8;
const total_users_count = 100;

// Update dashboard with data
document.getElementById('schemesCount').textContent = schemesCount;
document.getElementById('contactsCount').textContent = contactsCount;
document.getElementById('pendingRequests').textContent = pendingRequests;
document.getElementById('rejectedCount').textContent = rejectedCount;
document.getElementById('approvedCount').textContent = approvedCount;
document.getElementById('total_users_count').textContent = total_users_count;

